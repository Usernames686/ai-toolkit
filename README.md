# ai-toolkit

This repository is used to publish selected AI tooling and skills in a clearer, reusable layout.

## Included package

Current package:

- `slidecraft/`: a zero-dependency HTML presentation skill for generating and enhancing browser-based slide decks

## Provenance and attribution

The `slidecraft` package in this repository preserves the upstream attribution and license information included with the skill itself.

- Copyright holder named in the packaged license: `Maverick Gao`
- Upstream design-system attribution named in the packaged license and README: `Zara Zhang` and `frontend-slides`
- Original packaged license kept at `slidecraft/LICENSE`

For a repository-level summary of evidence and publishing notes, see `PROVENANCE.md`.

## Licensing note

This repository may contain materials under different licenses.

- the repository root currently includes its own top-level `LICENSE`
- the packaged `slidecraft` skill includes its own upstream `slidecraft/LICENSE`

Read both files before reuse or redistribution.

## What was changed in this publication

This repository snapshot publishes the skill as a standalone package and adds repository-facing documentation.

Changes made for this publication:

- copied the packaged `slidecraft` skill into this repository layout
- preserved the original `LICENSE`, `README.md`, `SKILL.md`, references, examples, and scripts
- added repository-level provenance notes so attribution and licensing are easy to verify

At this stage, no core behavior changes have been made to the packaged `slidecraft` source files in this repository snapshot.
